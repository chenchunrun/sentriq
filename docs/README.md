# Docs

The public repository keeps only a minimal docs index.

Current public usage is centered on the prototype CLI:

```bash
cp .env.example .env
pip install -r requirements.txt
python main.py --sample
```

For the current public entry points, see:

- `/README.md`
- `/QUICKSTART.md`
- `/CURRENT_STARTUP_GUIDE.md`
