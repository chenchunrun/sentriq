# Legacy Frontend Archive

The old standalone frontend has been archived to:

- `archived/web_dashboard_legacy/`

Do not use that archived tree for active development, Docker builds, or runtime startup.

The active frontend is:

- `services/web_dashboard/`

If older notes or reports mention `web_dashboard/`, interpret them as references to the archived tree at `archived/web_dashboard_legacy/`, unless they have already been updated to the active path.
