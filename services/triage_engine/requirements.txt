# Triage Engine Service Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# HTTP client for OpenAI-compatible decision/LLM endpoints (Laya, fallback providers)
httpx>=0.25.0

# Configuration (decision registry / threshold registry)
pyyaml>=6.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
